def set_pin_value(pin, value):
    # logging.info(f'Пин #{pin} <- {value}')
    print(f'Pin #{pin} <- {value}')


def setup_pin_out(pin):
    # logging.info(f'Пин #{pin} -- OUT')
    print(f'Pin #{pin} -- OUT')

# def setup_pin_in(pin):
#     # GPIO.setup(int(pin), GPIO.IN)
#     pass